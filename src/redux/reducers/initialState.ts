import { RootState, RootsState } from '../../models';

export const roots: RootsState = {
	isLoading: true,
	payload: null,
	error: null,
};

export const root: RootState = {
	isLoading: true,
	payload: null,
	error: null,
};
